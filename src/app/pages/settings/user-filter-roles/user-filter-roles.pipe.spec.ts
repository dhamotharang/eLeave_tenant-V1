import { UserFilterRolesPipe } from './user-filter-roles.pipe';

describe('UserFilterRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFilterRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
